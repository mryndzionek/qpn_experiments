/*****************************************************************************
* Model: dcf77.qm
* File:  ./decoder.c
*
* This code has been generated by QM tool (see state-machine.com/qm).
* DO NOT EDIT THIS FILE MANUALLY. All your changes will be lost.
*
* This program is open source software: you can redistribute it and/or
* modify it under the terms of the GNU General Public License as published
* by the Free Software Foundation.
*
* This program is distributed in the hope that it will be useful, but
* WITHOUT ANY WARRANTY; without even the implied warranty of MERCHANTABILITY
* or FITNESS FOR A PARTICULAR PURPOSE. See the GNU General Public License
* for more details.
*****************************************************************************/
/*${AOs::.::decoder.c} .....................................................*/
#include "qpn_port.h"
#include "bsp.h"
#include "decoder.h"
#include "phase_detector.h"

/*Q_DEFINE_THIS_FILE*/

#define DATE_DISP_MS      (BSP_TICKS_PER_SEC * 2)

/* PhaseDetector class declaration -----------------------------------------------*/
/*${AOs::Decoder} ..........................................................*/
typedef struct Decoder {
/* protected: */
    QMActive super;
} Decoder;

/* protected: */
static QState Decoder_initial(Decoder * const me);
static QState Decoder_LOCKING  (Decoder * const me);
static QState Decoder_LOCKING_e(Decoder * const me);
static QMState const Decoder_LOCKING_s = {
    (QMState const *)0, /* superstate (top) */
    Q_STATE_CAST(&Decoder_LOCKING),
    Q_ACTION_CAST(&Decoder_LOCKING_e),
    Q_ACTION_CAST(0), /* no exit action */
    Q_ACTION_CAST(0)  /* no intitial tran. */
};
static QState Decoder_SYNCING  (Decoder * const me);
static QState Decoder_SYNCING_e(Decoder * const me);
static QMState const Decoder_SYNCING_s = {
    (QMState const *)0, /* superstate (top) */
    Q_STATE_CAST(&Decoder_SYNCING),
    Q_ACTION_CAST(&Decoder_SYNCING_e),
    Q_ACTION_CAST(0), /* no exit action */
    Q_ACTION_CAST(0)  /* no intitial tran. */
};
static QState Decoder_DECODING  (Decoder * const me);
static QState Decoder_DECODING_e(Decoder * const me);
static QMState const Decoder_DECODING_s = {
    (QMState const *)0, /* superstate (top) */
    Q_STATE_CAST(&Decoder_DECODING),
    Q_ACTION_CAST(&Decoder_DECODING_e),
    Q_ACTION_CAST(0), /* no exit action */
    Q_ACTION_CAST(0)  /* no intitial tran. */
};
static QState Decoder_DATE  (Decoder * const me);
static QState Decoder_DATE_e(Decoder * const me);
static QState Decoder_DATE_x(Decoder * const me);
static QMState const Decoder_DATE_s = {
    &Decoder_DECODING_s, /* superstate */
    Q_STATE_CAST(&Decoder_DATE),
    Q_ACTION_CAST(&Decoder_DATE_e),
    Q_ACTION_CAST(&Decoder_DATE_x),
    Q_ACTION_CAST(0)  /* no intitial tran. */
};
static QState Decoder_YEAR  (Decoder * const me);
static QState Decoder_YEAR_e(Decoder * const me);
static QState Decoder_YEAR_x(Decoder * const me);
static QMState const Decoder_YEAR_s = {
    &Decoder_DECODING_s, /* superstate */
    Q_STATE_CAST(&Decoder_YEAR),
    Q_ACTION_CAST(&Decoder_YEAR_e),
    Q_ACTION_CAST(&Decoder_YEAR_x),
    Q_ACTION_CAST(0)  /* no intitial tran. */
};


/* Global objects ----------------------------------------------------------*/
Decoder AO_Decoder;

/* Blink class definition --------------------------------------------------*/
/*${AOs::Decoder_ctor} .....................................................*/
void Decoder_ctor(void) {
    QMActive_ctor(&AO_Decoder.super, Q_STATE_CAST(&Decoder_initial));
}
/*${AOs::Decoder} ..........................................................*/
/*${AOs::Decoder::SM} ......................................................*/
static QState Decoder_initial(Decoder * const me) {
    static struct {
        QMState const *target;
        QActionHandler act[2];
    } const tatbl_ = { /* transition-action table */
        &Decoder_LOCKING_s, /* target state */
        {
            Q_ACTION_CAST(&Decoder_LOCKING_e), /* entry */
            Q_ACTION_CAST(0) /* zero terminator */
        }
    };
    /* ${AOs::Decoder::SM::initial} */
    return QM_TRAN_INIT(&tatbl_);
}
/*${AOs::Decoder::SM::LOCKING} .............................................*/
/* ${AOs::Decoder::SM::LOCKING} */
static QState Decoder_LOCKING_e(Decoder * const me) {
    BSP_dispClear();
    (void)me; /* avoid compiler warning in case 'me' is not used */
    return QM_ENTRY(&Decoder_LOCKING_s);
}
/* ${AOs::Decoder::SM::LOCKING} */
static QState Decoder_LOCKING(Decoder * const me) {
    QState status_;
    switch (Q_SIG(me)) {
        /* ${AOs::Decoder::SM::LOCKING::PHASE_UPDATE} */
        case PHASE_UPDATE_SIG: {
            BSP_dispLocking();
            status_ = QM_HANDLED();
            break;
        }
        /* ${AOs::Decoder::SM::LOCKING::DCF_DATA} */
        case DCF_DATA_SIG: {
            static struct {
                QMState const *target;
                QActionHandler act[2];
            } const tatbl_ = { /* transition-action table */
                &Decoder_SYNCING_s, /* target state */
                {
                    Q_ACTION_CAST(&Decoder_SYNCING_e), /* entry */
                    Q_ACTION_CAST(0) /* zero terminator */
                }
            };
            status_ = QM_TRAN(&tatbl_);
            break;
        }
        default: {
            status_ = QM_SUPER();
            break;
        }
    }
    return status_;
}
/*${AOs::Decoder::SM::SYNCING} .............................................*/
/* ${AOs::Decoder::SM::SYNCING} */
static QState Decoder_SYNCING_e(Decoder * const me) {
    BSP_dispClear();
    (void)me; /* avoid compiler warning in case 'me' is not used */
    return QM_ENTRY(&Decoder_SYNCING_s);
}
/* ${AOs::Decoder::SM::SYNCING} */
static QState Decoder_SYNCING(Decoder * const me) {
    QState status_;
    switch (Q_SIG(me)) {
        /* ${AOs::Decoder::SM::SYNCING::DCF_DATA} */
        case DCF_DATA_SIG: {
            /* ${AOs::Decoder::SM::SYNCING::DCF_DATA::[BSP_dispSyncing~} */
            if (BSP_dispSyncing(Q_PAR(me)) != 0xFF) {
                static struct {
                    QMState const *target;
                    QActionHandler act[2];
                } const tatbl_ = { /* transition-action table */
                    &Decoder_DECODING_s, /* target state */
                    {
                        Q_ACTION_CAST(&Decoder_DECODING_e), /* entry */
                        Q_ACTION_CAST(0) /* zero terminator */
                    }
                };
                status_ = QM_TRAN(&tatbl_);
            }
            /* ${AOs::Decoder::SM::SYNCING::DCF_DATA::[else]} */
            else {
                status_ = QM_HANDLED();
            }
            break;
        }
        /* ${AOs::Decoder::SM::SYNCING::PHASE_UPDATE} */
        case PHASE_UPDATE_SIG: {
            status_ = QM_HANDLED();
            break;
        }
        default: {
            status_ = QM_SUPER();
            break;
        }
    }
    (void)me; /* avoid compiler warning in case 'me' is not used */
    return status_;
}
/*${AOs::Decoder::SM::DECODING} ............................................*/
/* ${AOs::Decoder::SM::DECODING} */
static QState Decoder_DECODING_e(Decoder * const me) {
    BSP_dispClear();
    (void)me; /* avoid compiler warning in case 'me' is not used */
    return QM_ENTRY(&Decoder_DECODING_s);
}
/* ${AOs::Decoder::SM::DECODING} */
static QState Decoder_DECODING(Decoder * const me) {
    QState status_;
    switch (Q_SIG(me)) {
        /* ${AOs::Decoder::SM::DECODING::DCF_DATA} */
        case DCF_DATA_SIG: {
            /* ${AOs::Decoder::SM::DECODING::DCF_DATA::[BSP_dispDecodin~} */
            if (BSP_dispDecoding(Q_PAR(me)) == 0xFF) {
                static struct {
                    QMState const *target;
                    QActionHandler act[2];
                } const tatbl_ = { /* transition-action table */
                    &Decoder_SYNCING_s, /* target state */
                    {
                        Q_ACTION_CAST(&Decoder_SYNCING_e), /* entry */
                        Q_ACTION_CAST(0) /* zero terminator */
                    }
                };
                status_ = QM_TRAN(&tatbl_);
            }
            /* ${AOs::Decoder::SM::DECODING::DCF_DATA::[else]} */
            else {
                BSP_dispTime();
                status_ = QM_HANDLED();
            }
            break;
        }
        /* ${AOs::Decoder::SM::DECODING::BUTTON_PRESSED} */
        case BUTTON_PRESSED_SIG: {
            static struct {
                QMState const *target;
                QActionHandler act[2];
            } const tatbl_ = { /* transition-action table */
                &Decoder_DATE_s, /* target state */
                {
                    Q_ACTION_CAST(&Decoder_DATE_e), /* entry */
                    Q_ACTION_CAST(0) /* zero terminator */
                }
            };
            status_ = QM_TRAN(&tatbl_);
            break;
        }
        default: {
            status_ = QM_SUPER();
            break;
        }
    }
    return status_;
}
/*${AOs::Decoder::SM::DECODING::DATE} ......................................*/
/* ${AOs::Decoder::SM::DECODING::DATE} */
static QState Decoder_DATE_e(Decoder * const me) {
    BSP_dispDate();
    QActive_arm((QActive *)me, DATE_DISP_MS);
    return QM_ENTRY(&Decoder_DATE_s);
}
/* ${AOs::Decoder::SM::DECODING::DATE} */
static QState Decoder_DATE_x(Decoder * const me) {
    QActive_disarm((QActive *)me);
    return QM_EXIT(&Decoder_DATE_s);
}
/* ${AOs::Decoder::SM::DECODING::DATE} */
static QState Decoder_DATE(Decoder * const me) {
    QState status_;
    switch (Q_SIG(me)) {
        /* ${AOs::Decoder::SM::DECODING::DATE::Q_TIMEOUT} */
        case Q_TIMEOUT_SIG: {
            static struct {
                QMState const *target;
                QActionHandler act[3];
            } const tatbl_ = { /* transition-action table */
                &Decoder_YEAR_s, /* target state */
                {
                    Q_ACTION_CAST(&Decoder_DATE_x), /* exit */
                    Q_ACTION_CAST(&Decoder_YEAR_e), /* entry */
                    Q_ACTION_CAST(0) /* zero terminator */
                }
            };
            status_ = QM_TRAN(&tatbl_);
            break;
        }
        /* ${AOs::Decoder::SM::DECODING::DATE::DCF_DATA} */
        case DCF_DATA_SIG: {
            BSP_dispDecoding(Q_PAR(me));
            status_ = QM_HANDLED();
            break;
        }
        default: {
            /* ${AOs::Decoder::SM::DECODING::DATE::CATCH_ALL} */
            status_ = QM_HANDLED();
            break;
        }
    }
    (void)me; /* avoid compiler warning in case 'me' is not used */
    return status_;
}
/*${AOs::Decoder::SM::DECODING::YEAR} ......................................*/
/* ${AOs::Decoder::SM::DECODING::YEAR} */
static QState Decoder_YEAR_e(Decoder * const me) {
    BSP_dispYear();
    QActive_arm((QActive *)me, DATE_DISP_MS);
    return QM_ENTRY(&Decoder_YEAR_s);
}
/* ${AOs::Decoder::SM::DECODING::YEAR} */
static QState Decoder_YEAR_x(Decoder * const me) {
    QActive_disarm((QActive *)me);
    return QM_EXIT(&Decoder_YEAR_s);
}
/* ${AOs::Decoder::SM::DECODING::YEAR} */
static QState Decoder_YEAR(Decoder * const me) {
    QState status_;
    switch (Q_SIG(me)) {
        /* ${AOs::Decoder::SM::DECODING::YEAR::Q_TIMEOUT} */
        case Q_TIMEOUT_SIG: {
            static struct {
                QMState const *target;
                QActionHandler act[2];
            } const tatbl_ = { /* transition-action table */
                &Decoder_DECODING_s, /* target state */
                {
                    Q_ACTION_CAST(&Decoder_YEAR_x), /* exit */
                    Q_ACTION_CAST(0) /* zero terminator */
                }
            };
            status_ = QM_TRAN(&tatbl_);
            break;
        }
        /* ${AOs::Decoder::SM::DECODING::YEAR::DCF_DATA} */
        case DCF_DATA_SIG: {
            BSP_dispDecoding(Q_PAR(me));
            status_ = QM_HANDLED();
            break;
        }
        default: {
            /* ${AOs::Decoder::SM::DECODING::YEAR::CATCH_ALL} */
            status_ = QM_HANDLED();
            break;
        }
    }
    (void)me; /* avoid compiler warning in case 'me' is not used */
    return status_;
}

